imports:
    - { resource: api.yml }
    - { resource: client.yml }

parameters:
    apiContentType: 'application/json'
    contentType: 'json'

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false



